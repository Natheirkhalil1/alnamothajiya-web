"use client"

import DashboardPage from "../page"

export default function SectionPage() {
  // All sections are rendered by the main dashboard page
  // which reads the section from the URL params
  return <DashboardPage />
}
