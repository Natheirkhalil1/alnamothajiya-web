# دليل شامل: كيف يعمل Page Builder؟ 🎨

## 📖 مقدمة بسيطة جداً

تخيل أنك تريد بناء صفحة ويب، لكن **بدون كتابة أي كود برمجي**!

Page Builder هو أداة تشبه **لعبة الليجو** 🧱 - تأخذ قطع جاهزة وترتبها كما تريد لبناء صفحة كاملة.

---

## 🎯 الفكرة الأساسية

### التشبيه بالحياة الواقعية

فكر في بناء صفحة ويب مثل **بناء منزل**:

```
🏠 المنزل = الصفحة الكاملة
🧱 الطوب = العناصر (Blocks)
🎨 الألوان والديكور = التصميم (Styles)
✨ الإضاءة والحركة = التأثيرات (Animations)
```

### ما هو "Block" (العنصر)؟

**Block** = قطعة جاهزة من الصفحة

أمثلة:
- 🖼️ **صورة كبيرة** في أعلى الصفحة
- 📝 **فقرة نصية** تشرح شيء
- 📊 **جدول أسعار**
- 📞 **نموذج تواصل**
- 🎬 **فيديو يوتيوب**

---

## 🏗️ كيف يعمل النظام؟

### 1️⃣ البنية الأساسية

```
Page Builder
│
├── 📦 Registry (السجل)
│   └── قائمة بجميع العناصر المتاحة (50+ عنصر)
│
├── 🗂️ Categories (الفئات)
│   └── تنظيم العناصر في 14 مجموعة
│
├── 🎨 Editor (المحرر)
│   └── واجهة لتعديل كل عنصر
│
└── 👁️ Viewer (العارض)
    └── كيف يظهر العنصر للزائر
```

---

## 📚 الأجزاء الرئيسية (بالتفصيل الممل!)

### الجزء 1: Registry (السجل) 📋

**ما هو؟**
- قائمة تحتوي على **جميع العناصر** المتاحة
- مثل **قائمة الطعام** في مطعم

**كيف يعمل؟**

```typescript
// ملف: registry.tsx

// لكل عنصر، يوجد شيئان:
{
  "hero-basic": {
    Editor: HeroBasicEditor,  // 🎨 شاشة التعديل
    View: HeroBasicView        // 👁️ كيف يظهر
  }
}
```

**مثال حقيقي:**

عندما تختار عنصر "Hero Basic" (قسم رئيسي):
1. النظام يبحث في السجل
2. يجد `HeroBasicEditor` (للتعديل)
3. يجد `HeroBasicView` (للعرض)
4. يعرض لك شاشة التعديل

**عدد العناصر المسجلة:** 50+ عنصر!

---

### الجزء 2: Categories (الفئات) 🗂️

**ما هي؟**
- تنظيم العناصر في مجموعات منطقية
- مثل **أقسام السوبرماركت** (خضار، لحوم، مشروبات...)

**الفئات الـ 14:**

#### 1. Hero Sections (الأقسام الرئيسية) 🎭
```
العناصر:
├── Hero Basic - قسم بسيط مع عنوان
├── Hero Slider - سلايدر صور متحرك
└── Hero Split - تصميم منقسم نصف/نصف
```

**متى تستخدمها؟**
- أول شيء يراه الزائر
- الانطباع الأول عن الصفحة

**مثال:**
```
┌─────────────────────────────────┐
│   🏫 مدرسة نموذجية للتربية   │
│      الخاصة                     │
│   [زر: اعرف المزيد]            │
└─────────────────────────────────┘
```

---

#### 2. Content (المحتوى) 📝

```
العناصر:
├── Section Header - عنوان قسم
├── Rich Text - نص غني بالتنسيق
├── Image + Text - صورة مع نص
├── Highlight Banner - شريط مميز
├── About Section - قسم من نحن
└── Info Card - بطاقة معلومات
```

**متى تستخدمها؟**
- لكتابة المحتوى الأساسي
- شرح الخدمات
- التعريف بالمدرسة

**مثال Rich Text:**
```
┌─────────────────────────────────┐
│ تأسست المدرسة عام 1985 في      │
│ مدينة عمان على مساحة 5000م²   │
│                                 │
│ نقدم برامج تعليمية متميزة...  │
└─────────────────────────────────┘
```

---

#### 3. Features & Services (المميزات والخدمات) ⭐

```
العناصر:
├── Programs Grid - شبكة البرامج
├── Services List - قائمة الخدمات
├── Curriculum Overview - المناهج
├── Departments Grid - الأقسام
└── Icon Points - نقاط بأيقونات
```

**متى تستخدمها؟**
- عرض ما تقدمه المدرسة
- إبراز المميزات

**مثال Programs Grid:**
```
┌────────┬────────┬────────┐
│ 📚     │ 🎨     │ 🏃     │
│ برنامج │ برنامج │ برنامج │
│ تعليمي │ فني    │ رياضي  │
└────────┴────────┴────────┘
```

---

#### 4. Social Proof (الإثبات الاجتماعي) 💬

```
العناصر:
├── Testimonials - آراء أولياء الأمور
├── Logos Strip - شعارات الشركاء
├── Statistics - إحصائيات
└── Social Icons - روابط التواصل
```

**متى تستخدمها؟**
- بناء الثقة
- إظهار النجاحات

**مثال Statistics:**
```
┌─────────────────────────────────┐
│  30+        500+       95%      │
│  سنة خبرة   طالب      نجاح     │
└─────────────────────────────────┘
```

---

#### 5. Media (الوسائط) 🎬

```
العناصر:
├── Gallery Grid - معرض شبكي
├── Gallery Masonry - معرض حر
├── Video Highlight - فيديو مميز
└── Video Embed - فيديو يوتيوب
```

**متى تستخدمها؟**
- عرض صور المدرسة
- فيديوهات توضيحية

---

#### 6. Team & People (الفريق) 👥

```
العناصر:
├── Staff Grid - شبكة الطاقم
└── Team List - قائمة الفريق
```

**مثال:**
```
┌────────┬────────┬────────┐
│ 👨‍🏫    │ 👩‍🏫    │ 👨‍💼    │
│ أحمد   │ فاطمة  │ محمد   │
│ معلم   │ معلمة  │ مدير   │
└────────┴────────┴────────┘
```

---

#### 7. Process & Timeline (العمليات) 📅

```
العناصر:
├── Horizontal Steps - خطوات أفقية
└── Timeline - جدول زمني
```

**متى تستخدمها؟**
- شرح خطوات التسجيل
- تاريخ المدرسة

**مثال Horizontal Steps:**
```
1️⃣ املأ النموذج → 2️⃣ انتظر الموافقة → 3️⃣ ادفع الرسوم → 4️⃣ ابدأ الدراسة
```

---

#### 8. Information (معلومات) ℹ️

```
العناصر:
├── FAQ - أسئلة شائعة
├── Downloads List - ملفات للتحميل
└── Pricing Table - جدول الأسعار
```

**مثال FAQ:**
```
❓ ما هي ساعات العمل؟
   ✅ من 7 صباحاً حتى 3 مساءً

❓ كم الرسوم الدراسية؟
   ✅ تبدأ من 500 دينار شهرياً
```

---

#### 9. Contact & Location (التواصل) 📞

```
العناصر:
├── Contact Section - معلومات الاتصال
└── Map - خريطة الموقع
```

---

#### 10. Call to Action (دعوة للعمل) 📢

```
العناصر:
├── CTA Strip - شريط دعوة
└── Newsletter Signup - اشتراك النشرة
```

**مثال:**
```
┌─────────────────────────────────┐
│  🎓 سجل الآن واحصل على خصم 20%  │
│        [زر: سجل الآن]           │
└─────────────────────────────────┘
```

---

#### 11. Layout (التخطيط) 📐

```
العناصر:
├── Columns - أعمدة (2-4 أعمدة)
├── Grid - شبكة
├── Divider - خط فاصل
└── Spacer - مسافة فارغة
```

**هذه مهمة جداً!** 🌟

**Columns** يسمح لك بـ:
- تقسيم الصفحة لأعمدة
- وضع عناصر داخل كل عمود
- **تداخل العناصر!**

**مثال:**
```
┌─────────────────────────────────┐
│  عمود 1      │  عمود 2          │
│  ────────    │  ────────        │
│  [صورة]      │  [نص]            │
│  [نص]        │  [زر]            │
└─────────────────────────────────┘
```

---

#### 12. Jobs & Employment (التوظيف) 💼

```
العناصر:
├── Jobs Listing - قائمة الوظائف
└── Job Application Form - نموذج التقديم
```

**ميزة خاصة:**
- يتصل تلقائياً بنظام التوظيف
- يعرض الوظائف من لوحة التحكم

---

#### 13. Forms (النماذج) 📋

```
العناصر:
├── Form Container - حاوية النموذج
├── Form Input - حقل نصي
├── Form Textarea - منطقة نص
├── Form Select - قائمة منسدلة
├── Form Checkbox - مربع اختيار
├── Form Radio - زر اختيار
├── Form Button - زر إرسال
└── Dynamic Form - نموذج من Dashboard
```

**كيف تستخدمها؟**
1. أضف Form Container
2. أضف الحقول بداخلها
3. أضف زر الإرسال

---

#### 14. Firebase Data (بيانات Firebase) 🔥

```
العناصر:
├── Firebase News - الأخبار
├── Firebase Achievements - الإنجازات
├── Firebase Gallery - المعرض
└── Firebase Hero Slider - السلايدر
```

**ميزة خاصة:**
- تعرض البيانات **تلقائياً** من قاعدة البيانات
- لا حاجة لإدخال يدوي

---

#### 15. Advanced (متقدم) 💻

```
العنصر:
└── Custom HTML - كود HTML مخصص
```

**للمطورين فقط!**

---

## 🎬 كيف تستخدم Page Builder؟ (خطوة بخطوة)

### السيناريو: إنشاء صفحة "من نحن"

#### الخطوة 1: افتح Page Builder

```
Dashboard → Pages → إضافة صفحة جديدة
```

#### الخطوة 2: اختر اللغة

```
┌─────────────────────────────────┐
│  [ العربية ]  [ English ]      │
└─────────────────────────────────┘
```

**مهم جداً!** 🚨
- كل لغة لها عناصرها الخاصة
- يمكنك نسخ من العربية للإنجليزية

#### الخطوة 3: أضف عنصر Hero

1. اضغط "إضافة بلوك جديد"
2. اختر فئة "Hero Sections"
3. اختر "Hero Basic"

**ماذا يحدث الآن؟**

```
النظام يفتح نافذة التعديل:

┌─────────────────────────────────┐
│  إعدادات Hero Basic            │
│  ────────────────────────────   │
│                                 │
│  [تبويب: المحتوى]              │
│  [تبويب: التصميم]              │
│  [تبويب: الحركة]               │
└─────────────────────────────────┘
```

#### الخطوة 4: املأ المحتوى

**تبويب المحتوى:**

```
العنوان (عربي): مدرسة نموذجية للتربية الخاصة
العنوان (إنجليزي): Model School for Special Education

الوصف (عربي): أكثر من 30 عاماً من التميز
الوصف (إنجليزي): More than 30 years of excellence

نص الزر: اعرف المزيد
رابط الزر: /about
```

#### الخطوة 5: خصص التصميم

**تبويب التصميم:**

```
┌─────────────────────────────────┐
│  الخلفية:                       │
│  ○ لون واحد                     │
│  ● تدرج لوني                    │
│                                 │
│  اللون الأول: #3B82F6 (أزرق)   │
│  اللون الثاني: #8B5CF6 (بنفسجي)│
│                                 │
│  لون النص: أبيض                │
│  المسافات: كبيرة                │
│  الظل: متوسط                    │
└─────────────────────────────────┘
```

#### الخطوة 6: أضف حركة

**تبويب الحركة:**

```
نوع الحركة: Fade In (ظهور تدريجي)
المدة: 0.5 ثانية
التأخير: 0 ثانية
```

#### الخطوة 7: احفظ وشاهد

اضغط خارج النافذة → العنصر يظهر في المعاينة!

---

## 🔧 الميزات المتقدمة

### 1. التداخل (Nesting)

**ما هو؟**
- وضع عناصر داخل عناصر أخرى

**مثال:**

```
Columns (عمودين)
├── عمود 1
│   ├── Image (صورة)
│   └── Rich Text (نص)
└── عمود 2
    ├── Section Header (عنوان)
    ├── Icon Points (نقاط)
    └── Button (زر)
```

**كيف؟**
1. أضف عنصر "Columns"
2. اختر عدد الأعمدة (2، 3، أو 4)
3. في كل عمود، اضغط "إضافة بلوك"
4. اختر العنصر المطلوب

---

### 2. السحب والإفلات (Drag & Drop)

```
قبل:
┌─────┐
│  A  │
├─────┤
│  B  │
├─────┤
│  C  │
└─────┘

اسحب B للأعلى ↑

بعد:
┌─────┐
│  B  │ ✅
├─────┤
│  A  │
├─────┤
│  C  │
└─────┘
```

---

### 3. النسخ (Clone)

```
لديك عنصر معقد؟
اضغط زر "نسخ" 📋
→ يتم إنشاء نسخة مطابقة تماماً!
```

**متى تستخدمها؟**
- عندما تريد عناصر متشابهة
- لتوفير الوقت

---

### 4. التحريك (Move)

```
أزرار:
↑ تحريك للأعلى
↓ تحريك للأسفل
```

---

### 5. الحذف (Delete)

```
🗑️ زر الحذف
→ تأكيد
→ العنصر يختفي
```

---

## 🎨 نظام التصميم (Styles)

### الألوان المتاحة

```javascript
// ألوان الخلفية
BLOCK_COLORS = {
  "أبيض": "bg-white",
  "رمادي فاتح": "bg-gray-50",
  "أزرق فاتح": "bg-blue-50",
  "أخضر فاتح": "bg-green-50",
  "أصفر فاتح": "bg-yellow-50",
  "أحمر فاتح": "bg-red-50",
  "بنفسجي فاتح": "bg-purple-50",
  "وردي فاتح": "bg-pink-50",
  // ... والمزيد
}

// ألوان النص
TEXT_COLORS = {
  "أسود": "text-gray-900",
  "رمادي": "text-gray-600",
  "أزرق": "text-blue-600",
  "أخضر": "text-green-600",
  // ... والمزيد
}
```

### المسافات (Spacing)

```javascript
SPACING_PRESETS = {
  "صغير جداً": "p-2",
  "صغير": "p-4",
  "متوسط": "p-8",
  "كبير": "p-12",
  "كبير جداً": "p-16"
}
```

### الظلال (Shadows)

```javascript
SHADOW_PRESETS = {
  "بدون": "shadow-none",
  "صغير": "shadow-sm",
  "متوسط": "shadow-md",
  "كبير": "shadow-lg",
  "كبير جداً": "shadow-xl"
}
```

### الحدود (Borders)

```javascript
BORDER_WIDTH_PRESETS = {
  "بدون": "border-0",
  "رفيع": "border",
  "متوسط": "border-2",
  "سميك": "border-4"
}

BORDER_RADIUS_PRESETS = {
  "بدون": "rounded-none",
  "صغير": "rounded-sm",
  "متوسط": "rounded-md",
  "كبير": "rounded-lg",
  "دائري": "rounded-full"
}
```

---

## ✨ نظام الحركات (Animations)

### الأنواع المتاحة

```javascript
ANIMATION_PRESETS = {
  "بدون": "none",
  "ظهور تدريجي": "fade-in",
  "انزلاق من اليمين": "slide-in-right",
  "انزلاق من اليسار": "slide-in-left",
  "انزلاق من الأعلى": "slide-in-top",
  "انزلاق من الأسفل": "slide-in-bottom",
  "تكبير": "scale-up",
  "دوران": "rotate-in",
  "ارتداد": "bounce-in"
}
```

### المدة (Duration)

```javascript
DURATION_PRESETS = {
  "سريع جداً": "0.2s",
  "سريع": "0.3s",
  "عادي": "0.5s",
  "بطيء": "0.8s",
  "بطيء جداً": "1.2s"
}
```

---

## 🔄 كيف يعمل النظام تقنياً؟ (للفضوليين!)

### 1. عند إضافة عنصر

```
المستخدم يضغط "إضافة بلوك"
        ↓
يفتح Modal (نافذة منبثقة)
        ↓
يعرض الفئات من block-categories.tsx
        ↓
المستخدم يختار عنصر (مثلاً: hero-basic)
        ↓
النظام ينادي createDefaultBlock("hero-basic")
        ↓
يتم إنشاء كائن Block جديد:
{
  id: "hero-basic-1234567890",
  kind: "hero-basic",
  titleAr: "",
  titleEn: "",
  descriptionAr: "",
  descriptionEn: "",
  styles: {},
  animations: {}
}
        ↓
يتم إضافته لـ blocksAr أو blocksEn
        ↓
يظهر في الصفحة!
```

### 2. عند التعديل

```
المستخدم يضغط "إعدادات"
        ↓
النظام يبحث عن Block بالـ ID
        ↓
يفتح Dialog مع 3 تبويبات
        ↓
تبويب المحتوى:
  - يستخدم HeroBasicEditor
  - يعرض حقول الإدخال
        ↓
المستخدم يكتب "مدرسة نموذجية"
        ↓
onChange يتم استدعاؤه
        ↓
Block يتم تحديثه:
{
  ...block,
  titleAr: "مدرسة نموذجية"
}
        ↓
الصفحة تُعاد رسمها
        ↓
التغيير يظهر فوراً!
```

### 3. عند العرض (للزائر)

```
الزائر يفتح الصفحة
        ↓
النظام يحدد اللغة (عربي/إنجليزي)
        ↓
يختار blocksAr أو blocksEn
        ↓
لكل Block:
  - يجلب View Component من Registry
  - HeroBasicView مثلاً
        ↓
يمرر البيانات:
<HeroBasicView block={block} />
        ↓
العنصر يُرسم على الشاشة
        ↓
الزائر يرى الصفحة الجميلة! 🎉
```

---

## 💾 كيف يتم الحفظ؟

### البنية في قاعدة البيانات

```json
{
  "id": "page-about",
  "slug": "about",
  "titleAr": "من نحن",
  "titleEn": "About Us",
  
  "blocksAr": [
    {
      "id": "hero-basic-123",
      "kind": "hero-basic",
      "titleAr": "مدرسة نموذجية",
      "descriptionAr": "أكثر من 30 عاماً",
      "styles": {
        "backgroundColor": "bg-gradient-to-r from-blue-600 to-purple-600",
        "textColor": "text-white"
      }
    },
    {
      "id": "rich-text-456",
      "kind": "rich-text",
      "contentAr": "تأسست المدرسة..."
    }
  ],
  
  "blocksEn": [
    {
      "id": "hero-basic-789",
      "kind": "hero-basic",
      "titleEn": "Model School",
      "descriptionEn": "More than 30 years"
    }
  ]
}
```

---

## 🎯 أمثلة عملية

### مثال 1: صفحة رئيسية بسيطة

```
1. Hero Slider (سلايدر صور)
   ↓
2. About Section (من نحن)
   ↓
3. Programs Grid (البرامج)
   ↓
4. Statistics (إحصائيات)
   ↓
5. Testimonials (آراء)
   ↓
6. Contact Section (تواصل)
```

### مثال 2: صفحة قسم

```
1. Hero Basic (عنوان القسم)
   ↓
2. Image + Text (صورة مع شرح)
   ↓
3. Columns (عمودين)
   ├── عمود 1: Icon Points (المميزات)
   └── عمود 2: Gallery Grid (صور)
   ↓
4. CTA Strip (سجل الآن)
```

### مثال 3: صفحة توظيف

```
1. Hero Basic (انضم لفريقنا)
   ↓
2. Rich Text (لماذا نحن؟)
   ↓
3. Jobs Listing (الوظائف المتاحة)
   ↓
4. Job Application Form (نموذج التقديم)
```

---

## 🚀 نصائح للاستخدام الأمثل

### ✅ افعل

1. **ابدأ بـ Hero** - دائماً ضع عنصر Hero في الأعلى
2. **استخدم Columns** - لتنظيم المحتوى جنباً إلى جنب
3. **أضف Spacer** - لإضافة مسافات بين الأقسام
4. **استخدم Divider** - لفصل الأقسام بصرياً
5. **اختبر على الجوال** - التصميم متجاوب تلقائياً

### ❌ لا تفعل

1. **لا تكثر من الألوان** - استخدم 2-3 ألوان فقط
2. **لا تضع نصوص طويلة** - قسمها لفقرات
3. **لا تنسى الحركات** - تجعل الصفحة حية
4. **لا تهمل النسخة الإنجليزية** - املأها أيضاً

---

## 🔍 استكشاف الأخطاء

### المشكلة: العنصر لا يظهر

```
الحل:
1. تأكد أنك في اللغة الصحيحة (عربي/إنجليزي)
2. تأكد أنك حفظت التغييرات
3. جرب تحديث الصفحة
```

### المشكلة: الألوان لا تتغير

```
الحل:
1. افتح تبويب "التصميم"
2. اختر اللون من القائمة
3. احفظ
```

### المشكلة: الحركة لا تعمل

```
الحل:
1. افتح تبويب "الحركة"
2. اختر نوع الحركة
3. تأكد أن المدة ليست 0
```

---

## 📊 ملخص سريع

```
Page Builder = أداة لبناء الصفحات بدون كود

المكونات:
├── 50+ عنصر جاهز
├── 14 فئة منظمة
├── 3 تبويبات لكل عنصر (محتوى، تصميم، حركة)
├── دعم كامل للعربية والإنجليزية
└── سحب وإفلات وتداخل

الاستخدام:
1. اختر عنصر
2. املأ المحتوى
3. خصص التصميم
4. أضف حركة
5. احفظ
6. استمتع! 🎉
```

---

## 🎓 الخلاصة

Page Builder هو **أداة قوية جداً** لكنها **سهلة الاستخدام**.

**لا تحتاج لمعرفة البرمجة!**

فقط:
- اختر العناصر
- رتبها
- خصصها
- واستمتع بصفحة جميلة! ✨

---

**هل لديك أسئلة؟** 🤔
- جرب بنفسك!
- اللعب بالعناصر أفضل طريقة للتعلم
- لا تخف من التجربة - يمكنك دائماً الحذف والبدء من جديد

**حظاً موفقاً! 🚀**
